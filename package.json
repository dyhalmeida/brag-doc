{
  "name": "brag-doc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "generate": "echo \"generate: not implemented yet\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0"
  }
}

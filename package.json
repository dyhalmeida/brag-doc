{
  "name": "brag-doc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "wins": "npm run build && node dist/edges/list-wins.js",
    "generate": "npm run build && node dist/edges/publish-site.js",
    "dashboard": "npm run build && node dist/edges/publish-dashboard.js",
    "reports": "npm run build && node dist/edges/publish-reports.js"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "marked": "^18.0.7",
    "yaml": "^2.9.0"
  }
}
